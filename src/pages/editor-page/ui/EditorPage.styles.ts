import { StyleSheet } from "react-native";

import { colors, radius, shadows, spacing } from "@/shared/config/theme";

export const styles = StyleSheet.create({
  centered: {
    alignItems: "center",
    flex: 1,
    gap: spacing.md,
    justifyContent: "center",
  },
  header: {
    alignItems: "center",
    backgroundColor: colors.accent,
    borderRadius: radius.lg,
    flexDirection: "row",
    gap: spacing.xs,
    marginBottom: spacing.xs,
    padding: spacing.xs,
    borderColor: "rgba(255,255,255,0.78)",
    borderWidth: 1,
    ...shadows.card,
  },
  headerButton: {
    minWidth: 58,
  },
  compactHeaderButton: {
    borderRadius: radius.md,
    minHeight: 36,
    paddingHorizontal: spacing.sm,
  },
  headerButtonLabel: { fontSize: 12, lineHeight: 16 },
  headerTitle: {
    alignItems: "center",
    flex: 1,
  },
  headerTitleText: { fontSize: 20, lineHeight: 25 },
  headerStatusText: { fontSize: 11, lineHeight: 14 },
  widgetAction: {
    flexDirection: "row",
    gap: spacing.sm,
    paddingVertical: spacing.sm,
  },
  previewStack: { position: "relative" },
  widgetSettingsOverlay: {
    backgroundColor: colors.surface,
    borderColor: colors.border,
    borderRadius: radius.lg,
    borderWidth: 1,
    elevation: 24,
    left: spacing.sm,
    maxHeight: 280,
    position: "absolute",
    right: spacing.sm,
    top: spacing.sm,
    zIndex: 50,
    ...shadows.card,
  },
  widgetSettingsContent: { padding: spacing.md },
  historyBar: {
    flexDirection: "row",
    justifyContent: "space-between",
    left: spacing.sm,
    position: "absolute",
    right: spacing.sm,
    top: spacing.sm,
    zIndex: 40,
  },
  historyArrow: { fontSize: 18, lineHeight: 20 },
  historyButton: {
    backgroundColor: "#3F756F",
    borderColor: "#315E59",
    borderRadius: radius.full,
    height: 38,
    minHeight: 38,
    paddingHorizontal: 0,
    width: 38,
  },
  historyButtonPressed: { backgroundColor: "#173F3B", opacity: 1 },
  toolbar: {
    backgroundColor: colors.surface,
    borderRadius: radius.lg,
    bottom: spacing.md,
    elevation: 20,
    flexGrow: 0,
    flexShrink: 0,
    left: spacing.lg,
    minHeight: 230,
    position: "absolute",
    right: spacing.lg,
    zIndex: 1000,
    borderColor: "rgba(255,255,255,0.9)",
    borderWidth: 1,
    ...shadows.card,
  },
  resizeHandle: {
    alignItems: "center",
    gap: spacing.xs,
    minHeight: 54,
    paddingHorizontal: spacing.md,
    paddingTop: spacing.sm,
  },
  resizeHandleBar: {
    backgroundColor: colors.border,
    borderRadius: radius.full,
    height: 5,
    width: 64,
  },
  tabScroller: {
    flexGrow: 0,
    flexShrink: 0,
    height: 84,
  },
  tabs: {
    flexDirection: "row",
    gap: spacing.sm,
    paddingHorizontal: spacing.md,
    paddingBottom: spacing.lg,
    paddingTop: spacing.sm,
  },
  panel: {
    gap: spacing.md,
    paddingHorizontal: spacing.md,
    paddingTop: spacing.lg,
    paddingBottom: spacing.xl,
  },
  controlGroup: {
    backgroundColor: colors.surfaceMuted,
    borderColor: colors.border,
    borderRadius: radius.md,
    borderWidth: 1,
    gap: spacing.md,
    padding: spacing.md,
  },
  sectionTitleRow: {
    alignItems: "center",
    flexDirection: "row",
    gap: spacing.sm,
  },
  toggleLabel: { flex: 1 },
  toggleRow: {
    alignItems: "center",
    flexDirection: "row",
    gap: spacing.md,
  },
  wrapRow: {
    flexDirection: "row",
    flexWrap: "wrap",
    gap: spacing.sm,
  },
  buttonRow: {
    flexDirection: "row",
    gap: spacing.sm,
  },
  rowItem: {
    flex: 1,
  },
});
